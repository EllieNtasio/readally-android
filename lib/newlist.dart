import 'package:flutter/material.dart';

class NewListScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('New List')),
      body: Center(
        child: Text('This is the New List screen'),
      ),
    );
  }
}
